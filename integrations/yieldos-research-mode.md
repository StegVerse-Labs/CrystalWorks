# YieldOS Research Mode (v0.1)

CrystalWorks programs should reference:
- run_record.run_type = "research"
- recipe_ref pointers (if any)
- lab_report references

Goal: keep experiment history queryable without storing sensitive raw data.
